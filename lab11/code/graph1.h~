#ifndef GRAPH_H
#define GRAPH_H

struct vertex{
	int data;
};

typedef struct vertex *Vertex;

struct edge{
	int start;
	int end;
};

typedef struct edge *Edge;

struct graph{
	int nume;
	struct vertex** vlist;
	struct edge* elist;
	int numv;
};

typedef struct graph *Graph;

#endif
