#include <stdio.h>
#include <stdlib.h>

int main(){
    int number = 20;
    FILE *fp = fopen("input.txt","w");
    int i;
    for(i=0;i<number;i++){
        int x = rand()%20;
        fprintf(fp,"%d\n",x);
    }
    return 0;
}
